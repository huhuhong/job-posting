import accountReducer from "./accountReducer";
import * as actions from "../actions/accountActions";

it("should add course when passed LOGIN_SUCCESS", () => {

});

it("should add course when passed LOGOUT_SUCCESS", () => {
    
});
